# Claude Code Adapter (Optional)

This is an optional Claude Code adapter location.

The canonical workflow is in `studio/`.

To use with Claude Code, copy the desired files from `studio/` into `.claude/`.
